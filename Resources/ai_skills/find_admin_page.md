---
description: 'Use when asked where in the admin UI something is managed.'
---
1. route_search by the feature keyword.
2. config_inspector if it is a setting (it returns settings_url).
3. Reply with the /admin/... path as a link, plus the menu breadcrumb if known.
